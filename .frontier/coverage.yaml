# EL DENOMINADOR — cuánto trabajo real existe FUERA de lo declarado.
# Medición: criterios de aceptación de SPEC-0001 que ningún change contract cita todavía.
count: "comm -23 <(grep -ohE '\"id\": \"AC-[0-9]{3}\"' .overlaykit/governance/specifications/SPEC-0001.json | grep -oE 'AC-[0-9]{3}' | sort -u) <(grep -ohE 'AC-[0-9]{3}' .overlaykit/governance/changes/*.json | sort -u) | wc -l"
